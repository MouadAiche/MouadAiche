name: Update Monkeytype Stats

on:
  workflow_dispatch:

  schedule:
    - cron: "0 */6 * * *"

permissions:
  contents: write

jobs:
  update-monkeytype:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Generate Monkeytype stats card
        env:
          MONKEYTYPE_APE_KEY: ${{ secrets.MONKEYTYPE_APE_KEY }}
        run: node scripts/update-monkeytype.js

      - name: Commit updated Monkeytype card
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add assets/monkeytype-stats.svg

          if git diff --cached --quiet; then
            echo "No Monkeytype changes"
            exit 0
          fi

          git commit -m "Update Monkeytype stats"
          git push
